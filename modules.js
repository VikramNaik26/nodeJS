const { john, harry, sayHi } = require("./globalVariable");
// console.log(module);

sayHi(john);

// to know more about package.json check out the blog post 'The basics of package json in node.js and npm'
